*한국어 · [English](INSTALL.en.md)*

# Docket 설치 안내

내게 할당된 Jira 티켓과, 그 티켓에 얽힌 Slack 스레드·Figma·PR을 메뉴바에서 한 번에 보는
macOS 앱입니다.

---

## 1. 설치

1. **`Docket 1.0.dmg` 다운로드** 후 더블클릭
2. 창이 열리면 **앱을 오른쪽 `Applications` 폴더로 드래그**
3. **터미널에 아래 한 줄을 실행** (최초 1회만)

   ```bash
   xattr -dr com.apple.quarantine "/Applications/Docket.app"
   ```

4. 응용 프로그램에서 실행 → **메뉴바에 체크리스트 아이콘**이 생깁니다

> **3번을 건너뛰면 "앱이 손상되었기 때문에 열 수 없습니다"가 뜹니다.**
> 앱이 실제로 손상된 게 아닙니다. 사내 배포용이라 Apple 공증을 받지 않았고, macOS가
> 공증되지 않은 앱을 이 문구로 거부합니다. 위 명령은 "인터넷에서 받은 파일" 표시를
> 지우는 것뿐입니다.

**응용 프로그램 폴더에 두세요.** 다운로드 폴더에서 실행하면 "맥 시작 시 실행"이 그 경로를
기억해서, 나중에 파일을 정리하면 자동 실행이 조용히 깨집니다.

---

## 2. 설정

메뉴바 아이콘 → 톱니바퀴를 눌러 설정을 엽니다. **Jira는 필수, Slack·GitHub는 선택**입니다
— 없어도 나머지는 정상 동작합니다.

### Jira (필수)

1. https://id.atlassian.com/manage-profile → **Security → API tokens** → 토큰 생성
2. 설정 → **Jira** 탭에 **사이트 주소**(예: `https://your-site.atlassian.net`), **이메일**, **API 토큰** 입력
3. **연결 테스트**

### Slack (선택 — 스레드를 보려면)

설정 → **Slack** 탭 → **`Slack 연결`** 클릭 → 브라우저에서 승인.

발급받을 토큰은 없습니다. 워크스페이스에 앱이 이미 등록돼 있어 승인만 하면 됩니다.
브라우저에 `Docket is connected`가 뜨면 완료입니다.

> 워크스페이스에 앱 승인 정책이 켜져 있으면 관리자 승인을 기다려야 할 수 있습니다.
> 연결하지 않아도 스레드 링크 붙여넣기는 동작합니다 — 내용과 답글 수만 빠진 링크 전용
> 카드로 남고, 나중에 연결하면 자동으로 채워집니다.

### GitHub (선택 — PR 버튼을 보려면)

1. github.com → Settings → Developer settings → **Personal access tokens (classic)**
2. 권한은 **`repo` 하나만** 체크 (하위 항목은 자동으로 함께 체크됩니다)
3. 설정 → **GitHub** 탭에 토큰 입력 → **연결 테스트**

> `repo`는 비공개 저장소를 읽기 위해 필요합니다. classic 토큰에는 "비공개 읽기 전용"
> 선택지가 없어 이것만으로 범위를 좁힐 수 없습니다. 만료일을 짧게 잡아두시길 권합니다.

---

## 3. 사용법

**메뉴바 아이콘 옆 숫자**는 확인하지 않은 티켓 수입니다. 새로 할당됐거나, 마지막으로 본 뒤
상태가 바뀌었거나, 안 읽은 Slack 답글이 있는 티켓이 세어집니다. **해당 티켓의 상세를 열면**
지워집니다.

**목록 정렬** — 마감(Target end)이 임박한 순, 같으면 우선순위 높은 순입니다. 완료된 티켓은
맨 아래로 내려갑니다. `D-3` 같은 배지가 남은 기간이고, 지났으면 빨간색입니다.

**티켓을 클릭**하면 상세가 열립니다. 여기에서:

- **PR / Jira / Figma 버튼** — Jira 설명에 링크가 있으면 자동으로 잡힙니다. PR은 티켓 키가
  PR 제목이나 브랜치명에 있으면 찾습니다.
- **Slack 스레드** — Jira 설명의 Slack 링크는 자동 연결됩니다. 없으면 **스레드 추가**에
  Slack 메시지 링크를 붙여넣으세요(Slack에서 메시지 우클릭 → Copy link). 답글 링크를 넣어도
  스레드 전체가 붙습니다.
- **Figma 링크 추가** — 자동으로 못 찾았거나 다른 걸 쓰고 싶을 때 직접 넣습니다.

**설정 → 일반**에서 바꿀 수 있는 것: 언어(한국어/English), 갱신 주기, 표시할 티켓 범위,
**맥 시작 시 실행**.

---

## 4. 문제가 생기면

| 증상 | 원인과 해결 |
|---|---|
| "손상되었기 때문에 열 수 없습니다" | 설치 3번을 건너뜀. 위 `xattr` 명령 실행 |
| 업데이트 후 키체인을 다시 물어봄 | 사내 배포판의 정상 동작입니다. **항상 허용**을 누르세요 |
| PR 버튼이 안 보임 | 티켓 키가 PR 제목·브랜치명에 없음. 또는 토큰이 저장소에 접근 못함 |
| PR 조회가 `SAML enforcement` 오류 | 토큰 목록에서 `Configure SSO` → 조직 `Authorize` |
| Slack 스레드가 안 붙음 | 설정 → Slack에서 **연결 해제 후 다시 연결** |
| 맥 시작 시 실행이 안 됨 | 시스템 설정 → 일반 → 로그인 항목에서 허용 확인 |
| 티켓이 안 보임 | 설정 → 일반의 **표시할 티켓**과 Jira 연결 테스트 확인 |

원인이 불분명하면 아래를 실행해 결과를 공유해 주세요.

```bash
log show --predicate 'subsystem == "dev.taetae.docket"' --last 30m
```

---

## 5. 개인정보

**모든 데이터는 본인 맥에만 저장됩니다.** 서버가 없어서 어디로도 전송되지 않습니다.

- 토큰: macOS 키체인
- 티켓·스레드 캐시: `~/Library/Application Support/Docket/`
- Slack은 **본인 권한 그대로** 조회합니다 — 공개 채널과 본인이 속한 비공개 채널·DM만 보입니다

---

## 변경 내역

| 버전 | 날짜 | 내용 |
|---|---|---|
| 1.0 | 2026-08-19 | 첫 배포 |
