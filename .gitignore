# Tuist generates these from Project.swift; only the manifests belong in the repo.
Derived/
*.xcodeproj
*.xcworkspace

# Build output
DerivedData/
build/
*.dmg

.DS_Store
dist/

# Local AI-plugin state; never part of the app.
.mswai/
.claude/
